- name: install dependencies
  become: yes
  apt:
    pkg:
      - automake
      - autoconf
      - libreadline-dev
      - libncurses-dev
      - libssl-dev
      - libyaml-dev
      - libxslt-dev
      - libffi-dev
      - libtool
      - unixodbc-dev
      - unzip
      - curl
    update_cache: yes

- include: nodejs.yml
- include: golang.yml
